export enum headerKeys {
  personal = 'personal',
  setting = 'setting',
  loginout = 'loginout',
}
export enum userReduce {
  token = 'Token',
  userInfo = 'UserInfo',
}
export enum appReduce {
  selectedKeys = 'SelectedKeys'
}